# 運用手順

## 公開ポリシー

本サイトは **検索エンジンには公開せず、直接リンク配布のみで運用する**。クライアント・エージェント等へは URL を直接共有する想定で、検索流入は前提としない。情報露出をコントロールするための方針。

### 具体策

| 対象          | 設定                                                                     |
| ------------- | ------------------------------------------------------------------------ |
| robots.txt    | 全クローラに `Disallow: /`（`src/app/robots.ts`）                        |
| メタタグ      | `robots: { index: false, follow: false }`（`src/app/layout.tsx`）        |
| sitemap.xml   | 非生成（クローラ誘導が唯一の用途のため `src/app/sitemap.ts` を削除済み） |
| OGP / JSON-LD | **維持**。検索インデックスとは独立して、直リンク共有時のプレビューに有効 |

### 補足

- ドメインは `e2life.dev`（Vercel 管理、HTTPS 自動）。検索非公開でも独自ドメインで運用する
- Lighthouse の SEO スコアは `is-crawlable` の意図的失敗により低下するが、これは方針通り。他の SEO 監査・Performance / Accessibility / Best Practices は基準（90 以上）を維持する（`.lighthouserc.json` は SEO 総合ゲートを撤廃し、オンページ SEO 監査を個別にチェック）
- 将来「検索公開」に方針転換する場合は、上記の `robots.ts` / `layout.tsx` を戻し、sitemap を復活させる

## コンテンツ更新フロー

### MVP: 手動更新

```
コンテンツ修正（src/lib/data/*.ts）
  → feature ブランチ作成
  → PR 作成
  → テスト自動実行
  → レビュー
  → main にマージ
  → Vercel 自動デプロイ
```

更新対象:

| データ     | ファイル                 | 更新頻度     |
| ---------- | ------------------------ | ------------ |
| スキル情報 | `src/lib/data/skills.ts` | 新技術習得時 |
| 職務経歴   | `src/lib/data/career.ts` | 案件変更時   |
| サイト情報 | `src/lib/data/site.ts`   | まれ         |

## 監視

### Vercel Analytics

- Vercel ダッシュボードで確認
- リアルタイムのアクセス状況
- デプロイ履歴とステータス
- エラーログ

### Google Analytics

| 指標               | 確認頻度 | 目的                          |
| ------------------ | -------- | ----------------------------- |
| PV（ページビュー） | 週 1 回  | 各ページのアクセス状況を把握  |
| PDF ダウンロード数 | 週 1 回  | CTA の効果測定                |
| 問い合わせ数       | 都度     | コンバージョン率の把握        |
| 流入元             | 月 1 回  | どこからアクセスがあるか      |
| デバイス比率       | 月 1 回  | デスクトップ / モバイルの比率 |

### パフォーマンス監視

- Lighthouse CI を PR ごとに自動実行
- Core Web Vitals (LCP, CLS, INP) を継続モニタリング
- スコアが基準値を下回った場合は PR にコメントで警告

## 障害対応

### 障害検知

| 手段                  | 検知できるもの                                   |
| --------------------- | ------------------------------------------------ |
| Vercel ダッシュボード | デプロイ失敗、ランタイムエラー、関数タイムアウト |
| Google Analytics      | アクセス数の異常な減少                           |
| ユーザー報告          | 表示崩れ、フォーム送信失敗                       |

### 対応フロー

```
障害検知
  → 影響範囲の確認
  → 緊急度判断
  → 高: Vercel ダッシュボードから即座にロールバック
  → 中: git revert で修正 PR 作成 → マージ → 自動デプロイ
  → 低: 通常の修正フローで対応
```

### ロールバック

deploy.md の「ロールバック手順」を参照。

## コスト管理

### 固定費

| 項目                  | 費用       | 備考              |
| --------------------- | ---------- | ----------------- |
| ドメイン (e2life.dev) | 年 $12〜15 | Vercel 経由で取得 |
| Vercel Hobby プラン   | 無料       | 個人利用          |

### 無料枠の制限（Vercel Hobby）

| リソース                 | 上限              |
| ------------------------ | ----------------- |
| 帯域幅                   | 100 GB / 月       |
| ビルド時間               | 6,000 分 / 月     |
| Serverless Function 実行 | 100 GB-hours / 月 |
| Image Optimization       | 1,000 枚 / 月     |

個人サイトの規模では無料枠を超える可能性は低い。超えた場合は Pro プラン（$20/月）への切り替えを検討する。

### 外部サービス

| サービス          | 費用 | 備考                                    |
| ----------------- | ---- | --------------------------------------- |
| Google Analytics  | 無料 |                                         |
| reCAPTCHA v3      | 無料 | 月 100 万回まで                         |
| Google Sheets API | 無料 | 日 500 リクエストまで（フェーズ 2）     |
| GitHub API        | 無料 | 時間 5,000 リクエストまで（フェーズ 2） |
